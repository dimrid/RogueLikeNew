using UnityEngine;
using UnityEngine.Events;

public class HealthPicker : MonoBehaviour
{
    //private float _countHealther;
    //public event UnityAction<int> ScoreChange;
    //private HealthManager _maxHealth;
    //private void OnTriggerEnter2D(Collider2D other) {
    //    if (other.TryGetComponent(out HealthPoint healthPoint)) {
    //        _maxHealth += healthPoint.Score;
    //            ScoreChange?.Invoke(_countHealther);
    //            Destroy(healthPoint.gameObject);
    //        }
    //}
}
